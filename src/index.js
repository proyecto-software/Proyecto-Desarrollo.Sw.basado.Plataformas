import React from "react";
import ReactDOM  from "react-dom";
import App from './components/App';/* Traemos ahora el componente App al index,js y al render. */
ReactDOM.render(<App/>, document.getElementById('app'));
